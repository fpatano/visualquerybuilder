# Node.js Configuration
NODE_ENV=production
PORT=3000
APP_BASE_PATH=/
LOG_LEVEL=info

# Application Metadata
APP_VERSION=1.0.0

# Databricks Configuration
# Replace these with your actual Databricks workspace details
DATABRICKS_HOST=https://your-workspace.cloud.databricks.com
DATABRICKS_TOKEN=your-databricks-token-here
DATABRICKS_WAREHOUSE_ID=your-warehouse-id

# Security & CORS (optional)
CORS_ORIGIN=

# Feature Flags for Development/Testing
FEATURE_SKIP_READINESS=
FEATURE_ALLOW_NO_DBRX=

# Instructions:
# 1. Copy this file to .env: cp env.example .env
# 2. Replace the placeholder values with your actual Databricks credentials
# 3. To get your token: Databricks workspace > User Settings > Access Tokens > Generate New Token
# 4. To get your workspace URL: Look at your browser URL when in Databricks workspace
# 5. To get warehouse ID: Databricks workspace > SQL > Warehouses > Copy the ID from your warehouse
#
# Deployment Variables (handled by Databricks Apps):
# - PORT: Will be set by Databricks Apps runtime
# - APP_BASE_PATH: Will be set if your app is not mounted at root
# - NODE_ENV: Should be 'production' for deployed apps
#
# Feature Flags:
# - FEATURE_SKIP_READINESS=1: Skip dependency checks in readiness endpoint
# - FEATURE_ALLOW_NO_DBRX=1: Allow app to start without Databricks credentials (dev only)